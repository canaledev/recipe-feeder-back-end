feat: initialize base project architecture
====================
## User Story

**As a** developer working on Feedy,
**I want** a fully configured base project architecture,
**so that** I can build features on a solid, consistent, and scalable foundation without setup friction.

---

## Context

The repository currently has no source code. This issue tracks the initialization of the full frontend stack as defined in the technical specifications: React 18 + Vite + TypeScript, Tailwind CSS with design tokens, TanStack Query, React Context API, Vitest, and PWA support.

---

## Acceptance Criteria

- [ ] Running `npm run dev` starts the development server without errors
- [ ] Running `npm run build` produces a valid production build with no TypeScript errors
- [ ] Running `npm run test` executes the test suite via Vitest
- [ ] Running `npm run lint` passes with no errors
- [ ] Tailwind CSS is configured with the project design tokens (organic color palette: emerald greens, terracottas, ochres)
- [ ] Google Fonts loads **Bitter** (headings) and **Inter** (body) correctly
- [ ] A PWA `manifest.json` is present and the app is installable from a mobile browser
- [ ] A Service Worker is registered for offline support
- [ ] The root layout constrains the main container to `max-w-md` on desktop
- [ ] The feature-based folder structure (`src/features/`, `src/components/`, `src/hooks/`, `src/lib/`, `src/types/`) is in place
- [ ] A centralized API client module exists in `src/lib/api.ts`
- [ ] TanStack Query `QueryClientProvider` is wired up at the app root
- [ ] A global React Context provider for client-side state is scaffolded
- [ ] At least one unit test exists and passes (smoke test for the App component)

---

## Technical Tasks

- [ ] Initialize project with `npm create vite` (React + TypeScript template)
- [ ] Install and configure Tailwind CSS with custom design tokens
- [ ] Install TanStack Query and configure `QueryClientProvider`
- [ ] Install Lucide-React and Phosphor Icons
- [ ] Install Headless UI or Radix UI
- [ ] Set up Google Fonts (Bitter + Inter) via `index.html`
- [ ] Configure Vitest and add smoke test
- [ ] Configure ESLint + Prettier
- [ ] Set up PWA via `vite-plugin-pwa` (manifest + Service Worker)
- [ ] Scaffold feature-based folder structure with placeholder index files
- [ ] Create centralized API client (`src/lib/api.ts`) with base URL from `VITE_API_BASE_URL`
- [ ] Wire up `QueryClientProvider` and global Context in `src/main.tsx`
- [ ] Add `.env.example` with required environment variables
- [ ] Update `CHANGELOG.md` with this change under `[Unreleased]`

---

## Definition of Done

All acceptance criteria are met, all unit tests pass, and a Pull Request is open for review.
--------------------
feat: user registration screen with email and social OAuth providers
====================
´╗┐## User Story

As a new user, I want to create a Feedy account using my email and password or via a trusted social provider (Google, Apple, or Facebook), so that I can access the personalized Smart Feed quickly and securely.

## Acceptance Criteria

### App routing based on auth state
- [x] App shows a Splash Screen on first load (authStatus = unknown)
- [x] Splash Screen routes to RegisterPage when user is not authenticated
- [x] Splash Screen routes to Smart Feed placeholder when user is authenticated
- [x] RegisterPage is shown automatically when authStatus = unauthenticated

### Splash Screen (dev-mode auth simulation)
- [x] Displays Feedy brand heading and tagline
- [x] Shows two dev-mode buttons: "I am already registered" and "I am not registered yet"
- [x] "Already registered" button sets authStatus to authenticated and shows home screen
- [x] "Not registered yet" button sets authStatus to unauthenticated and shows RegisterPage
- [ ] Replace dev buttons with real session check when backend auth is ready

### Email / Password registration
- [x] Form collects: full name, email address, password, password confirmation
- [x] Email field validates RFC 5322 format on blur
- [x] Password must meet: >= 12 characters, at least one uppercase, one lowercase, one digit, and one special character
- [x] Real-time password strength indicator (Weak / Fair / Strong / Very Strong)
- [x] Show / hide password toggle on both password fields
- [x] Password confirmation must match before submission is allowed
- [x] autocomplete="new-password" on password inputs to prevent autofill leaking
- [x] Error messages never reveal whether an email is already registered (prevents enumeration attacks)
- [x] Submission is disabled while a request is in flight (prevents double-submit)

### Social OAuth providers
- [x] Google Sign-In button (top priority - widest adoption)
- [x] Apple Sign-In button (required for PWA / future App Store compliance)
- [x] Facebook Login button
- [x] Each OAuth button shows the provider logo + "Continue with Provider" label
- [x] OAuth redirect/popup failure surfaces a user-friendly error message

### Terms and Privacy
- [x] Checkbox: "I agree to the Terms of Service and Privacy Policy" (required before submit)
- [x] Links open the documents in a new tab

### Navigation
- [x] "Already have an account? Log in" link navigates to the future login screen
- [x] Successful registration redirects to the home screen (Smart Feed)

### Security non-negotiables
- [x] Passwords are never stored in component state beyond the controlled input lifetime
- [x] No credentials are written to console.log or error tracking payloads
- [x] All communication goes through the centralized API client (src/lib/api.ts)
- [x] On backend 429 (rate limit), display "Too many attempts. Please try again later."

### Design / UX
- [x] Follows the organic Feedy design system (Bitter headings, emerald/terracotta/ochre palette)
- [x] Mobile-first layout constrained to max-w-md
- [x] Loading spinner while request is in flight
- [x] Fully accessible: all inputs have visible labels and aria-describedby for error messages

## Out of scope (this issue)
- Login screen
- Password reset / forgot password flow
- Email verification flow (backend concern for MVP)
- Real backend session check (replace dev buttons when backend is ready)

## Technical notes
- Auth state: authStatus ('unknown' | 'authenticated' | 'unauthenticated') in AppContext
- Feature folder: src/app/features/auth/
- Components: RegisterPage, EmailRegisterForm, SocialAuthButton, PasswordStrengthIndicator, SplashScreen
- Types: RegistrationCredentials, AuthResponse, SocialProvider, PasswordStrength, AuthStatus
- Tests: src/tests/features/auth/ + src/tests/app/SplashScreen.test.tsx

--------------------
feat: implement full app UI from design handoff
====================
Implement all screens and design system from the Claude Design handoff bundle. Screens: Home feed, Recipe Detail, Onboarding (4-step), Browse Playlists, My Collections, Create Playlist, Favorites. Design system: new Tailwind color tokens (feedy-lime #BDF367, feedy-lime-bright #B1F21A, feedy-orange #FF9966, feedy-surface #F8F8F8), hamburger header + slide-in side menu, Bitter serif headings.
--------------------
feat: Browse & Subscribe to Recipe Playlists
====================
## User Story

As a user, I want to browse available recipe playlists and subscribe to them, so that I can follow curated meal sequences that match my interests.

## Acceptance Criteria

- [ ] The screen displays a list/grid of available playlists with cover image, title, recipe count, and a brief description.
- [ ] A search bar allows the user to find playlists by name, ingredient, cuisine type, or tag.
- [ ] The user can sort results by: relevance, popularity, newest, and match percentage.
- [ ] The user can filter results by: dietary regime (Vegan, Keto, Celiac, Paleo, etc.), difficulty, and duration.
- [ ] Each playlist card shows a Subscribe / Subscribed toggle button.
- [ ] Subscribing to a playlist is reflected immediately in the UI (optimistic update).
- [ ] Empty state is shown when no results match the search/filter criteria.

## UX Notes

- Mobile-first layout (`max-w-md`).
- Skeleton loaders while data is fetching.
- Search input should debounce requests (300 ms).

## Out of Scope

- Creating playlists from this screen.
- Viewing individual playlist contents (links to detail screen).
--------------------
feat: My Subscribed Playlists Screen
====================
## User Story

As a user, I want to see all the playlists I am subscribed to in one place, so that I can easily manage and navigate my curated meal sequences.

## Acceptance Criteria

- [ ] The screen lists all playlists the current user is subscribed to (cover image, title, recipe count, progress indicator).
- [ ] The user can filter the list by: dietary regime, difficulty, and completion status (in progress / not started / completed).
- [ ] The user can sort the list by: subscription date (newest/oldest), alphabetical, and match percentage.
- [ ] Tapping a playlist navigates to the playlist detail screen.
- [ ] The user can unsubscribe from a playlist directly from this screen (with a confirmation gesture/dialog).
- [ ] Empty state is shown when the user has no subscriptions, with a CTA to browse playlists.

## UX Notes

- Mobile-first layout (`max-w-md`).
- Skeleton loaders while data is fetching.
- Unsubscribe action should use a swipe-to-reveal gesture or long-press menu on mobile.

## Out of Scope

- Editing playlist contents.
- Creating new playlists from this screen.
--------------------
feat: Create Custom Playlist Screen
====================
## User Story

As a user, I want to create my own recipe playlist by searching and adding recipes with filters and ordering, so that I can build personalized meal sequences tailored to my preferences.

## Acceptance Criteria

- [ ] The screen has a header section to set the playlist name, optional description, and cover image.
- [ ] A recipe search panel lets the user find recipes by: name, ingredient, tag, dietary regime, and difficulty.
- [ ] The user can sort search results by: match percentage, prep time, difficulty, and newest.
- [ ] The user can add a recipe to the playlist with a single tap.
- [ ] Added recipes appear in an ordered list below (or in a separate panel); the user can drag-to-reorder them.
- [ ] The user can remove a recipe from the playlist before saving.
- [ ] The playlist can be saved as private or public.
- [ ] The screen validates that the playlist has at least one recipe and a name before allowing save.
- [ ] On successful save, the user is navigated to the new playlist's detail screen.

## UX Notes

- Mobile-first layout (`max-w-md`).
- Two-panel layout (search + selected list) or tab-based navigation between "Search" and "My Playlist".
- Haptic feedback on recipe add/remove and reorder.

## Out of Scope

- Editing an already published playlist (separate screen/flow).
- Collaborative playlists.

--------------------
feat: Favorite Recipes Screen
====================
## User Story

As a user, I want to view and organize my favorite recipes, so that I can quickly find and revisit the recipes I love most.

## Acceptance Criteria

- [ ] The screen displays all recipes the user has marked as favorites (cover image, title, match percentage, difficulty, prep time).
- [ ] The user can search favorites by recipe name or ingredient.
- [ ] The user can filter favorites by: dietary regime, difficulty, and prep time range.
- [ ] The user can sort favorites by: date added (newest/oldest), match percentage, alphabetical, and prep time.
- [ ] The user can organize favorites into named folders (collections).
- [ ] The user can create, rename, and delete folders.
- [ ] The user can move a recipe from one folder to another.
- [ ] The user can remove a recipe from favorites directly from this screen.
- [ ] Empty state is shown when the user has no favorites, with a CTA to explore the feed.

## UX Notes

- Mobile-first layout (`max-w-md`).
- Folder view and flat list view toggle.
- Skeleton loaders while data is fetching.
- Swipe-to-reveal or long-press context menu for remove / move-to-folder actions.

## Out of Scope

- Sharing folders publicly.
- Collaborative folders.

--------------------
feat: AI Recipe Suggestion Screen
====================
## User Story

As a user, I want the system to suggest a recipe tailored to my stored taste profile and any preferences I can add on the spot, so that I can get instant, personalized inspiration when I don't know what to cook.

## Acceptance Criteria

- [ ] The screen shows a "Suggest a recipe" CTA that triggers the suggestion engine.
- [ ] Before or alongside the suggestion, the user can add one-time contextual preferences: available ingredients, mood/craving tags (e.g., "comfort food", "quick", "light"), and max prep time.
- [ ] One-time preferences do not overwrite the stored profile ÔÇö they apply only to the current request.
- [ ] The system returns a single recipe suggestion with a match explanation ("Based on your Keto preference and 'quick' tag").
- [ ] The user can accept the suggestion (navigate to recipe detail) or request a new suggestion.
- [ ] The user can save the suggested recipe to favorites directly from this screen.
- [ ] If no suitable recipe is found, a friendly empty state is shown with suggestions to relax filters.

## UX Notes

- Mobile-first layout (`max-w-md`).
- The suggestion reveal should feel like a moment of delight ÔÇö consider a card-flip or fade-in animation.
- Loading state must be explicit (skeleton or spinner with a short witty loading message).
- One-time preference chips should be easy to add and dismiss.

## Out of Scope

- Multi-recipe suggestion lists (single suggestion per request for MVP).
- Modifying the stored profile from this screen (that belongs in the onboarding/profile edit flow).

--------------------
feat: view playlist contents from playlist card
====================
´╗┐## User Story

**As a** user browsing playlists,
**I want to** tap "Ver" on a playlist card and see all the recipes it contains,
**So that** I can evaluate a playlist's content before deciding to subscribe.

---

## Acceptance Criteria

- [ ] Each playlist card in `BrowsePlaylistsScreen` has a "Ver" button
- [ ] Tapping "Ver" opens a new `PlaylistDetailScreen`
- [ ] The detail screen shows the playlist header: cover image, name, author, description, and key stats (recipe count, avg prep time, difficulty, rating)
- [ ] The detail screen lists all recipes in the playlist with: title, thumbnail, prep time, difficulty, and match percentage
- [ ] Each recipe in the list is tappable and navigates to `RecipeDetailScreen`
- [ ] A back button returns the user to `BrowsePlaylistsScreen`
- [ ] Loading and empty states are handled gracefully

---

## Technical Notes

- New screen: `PlaylistDetailScreen` inside `features/playlists/`
- New hook: `usePlaylistDetail(id)` using TanStack Query `useQuery`
- New service call: `getPlaylistDetail(id)` ÔåÆ `GET /playlists/{id}` (returns playlist + recipes)
- New types: `PlaylistItem` and `PlaylistDetail extends Playlist`
- Extend mock API client to handle `GET /playlists/{id}`
- Extend `Screen` union and `NavigateFn` in `App.tsx`

--------------------
feat: implement internationalization (i18n) with settings menu
====================
´╗┐## User Story
As a user, I want to be able to change the application language in a settings menu so that I can use the app in my preferred native language (English, Spanish, Portuguese, French, or Hindi).

## Context
The application currently only supports English. To reach a global audience and provide an organic, personalized experience, we need to implement a robust i18n infrastructure that handles both static UI labels and future localized dynamic content from the backend.

## Acceptance Criteria
- [ ] **Auto-detection**: On first load, the app detects the system language using 
avigator.languages and sets it as active (if supported). Default is English.
- [ ] **Settings Screen**: A new screen for "Settings" accessible via a gear icon in the AppHeader or SideMenu.
- [ ] **Language Selector**: The settings screen includes a list of 5 languages (English, Spanish, Portuguese, French, Hindi) showing their native names and a flag icon (SVG-based).
- [ ] **Persistence**: The selected language is saved in localStorage and persists across sessions.
- [ ] **API Integration**: All outgoing requests from ^Gpi.ts include the Accept-Language header with the current language code.
- [ ] **UI Translation**: All static labels, buttons, and menu items are translated into the 5 base languages using i18next.
- [ ] **Lazy Loading**: Translation files are loaded on-demand from public/locales/ to keep the bundle size small.

## Technical Notes
- **Dependencies**: Add i18next, eact-i18next, i18next-browser-languagedetector, i18next-http-backend.
- **Icons**: Use a resilient SVG-based library for flags (e.g., lag-icons).
- **State Management**: Integrate i18next with the existing React context if needed, or use the useTranslation hook directly.
- **Routing**: Update App.tsx to include the settings screen in the manual router.
- **Files to create**:
    - src/app/i18n.ts (Configuration)
    - src/app/features/settings/SettingsScreen.tsx
    - public/locales/{en,es,pt,fr,hi}/translation.json
- **Files to modify**:
    - src/app/lib/api.ts (Add headers)
    - src/app/App.tsx (Add route)
    - src/app/components/AppLayout/SideMenu.tsx (Add navigation link)
    - package.json (Dependencies)

## Out of Scope
- Localizing existing mock recipe data (remains in original language for now).
- RTL (Right-to-Left) support for this iteration.

--------------------
feat(i18n): internationalization with Settings screen (#18)
====================
´╗┐Closes #18

## Summary
- Add i18next infrastructure with auto-detection of system language (
avigator.languages), localStorage persistence, and lazy-loaded translation files from public/locales/{lng}/translation.json
- New Settings screen accessible from the side menu with a language selector showing native names and SVG flag icons for English, Espanol, Portugues, Francais, and Hindi
- All API requests now include the Accept-Language header with the active language code, and all side menu nav labels are translated via t() keys

## Test plan
- [ ] All tests pass (
pm run test:run) -- 84/84
- [ ] Production build succeeds (
pm run build)
- [ ] Manual: app loads in English by default (or matches system language)
- [ ] Manual: open Settings via side menu -- all 5 languages appear with flags
- [ ] Manual: select Spanish -- nav labels switch to Spanish; reload -- Spanish persists
- [ ] Manual: DevTools Network -- any API call shows Accept-Language: es header

Generated with Claude Code

--------------------
